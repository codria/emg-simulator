# emg-simulator dependencies.
#
# `pip install -r requirements.txt` is enough to run the app AND the tests on a
# fresh clone — see README "Quick start". Tested on Python 3.10–3.13.

# --- core: kinematics + signal processing + tests ---
numpy
scipy
pytest

# --- rendering: PySide6 (Qt) + PyQtGraph GL scene (required to run the app) ---
pyqtgraph
PyOpenGL
PySide6            # Qt binding for pyqtgraph (LGPL; PyQt6 also works)

# --- acquisition: BioRadio .NET SDK via pythonnet (Windows only, OPTIONAL) ---
# Imported lazily in emg_sim/acquisition/bioradio.py (start()), so the app and the
# tests run without it / without a device. Official BioRadio Python example pins 3.0.5.
pythonnet==3.0.5  ; sys_platform == "win32"
